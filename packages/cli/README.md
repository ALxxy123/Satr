# @satr/cli

Command-line interface for Satr.

Provides `satr init`, `satr scan`, `satr rules list`, and `satr explain` for local-first
sensitive data checks in development and CI.

See the repository README for full v0.1 usage, limitations, and security model.
